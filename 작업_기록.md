# 3D 에셋 모델 뷰어 작업 기록

작성일: 2026-07-28

## 1. 요청 내용

- `현2222.glb` 파일을 웹에서 볼 수 있는 로컬 3D 뷰어 제작
- `<model-viewer>` 기반으로 제작
- 로컬 서버는 Python 사용
- 3D 모델 옆에 설명을 표시하는 화면 구성 추가
- 화면에 표시되는 모델 이름을 **3D 에셋 모델**로 변경

## 2. 만들어진 구성

| 파일 | 역할 |
| --- | --- |
| `index.html` | 3D 뷰어 및 설명 패널 화면 |
| `styles.css` | 반응형 2열 레이아웃 및 화면 스타일 |
| `app.js` | 자동 회전 켜기/끄기, 시점 초기화 기능 |
| `server.py` | Python 로컬 웹 서버 |
| `뷰어_실행.bat` | Blender 내장 Python으로 뷰어를 실행하는 파일 |
| `model-viewer.min.js` | 외부 연결 없이 사용할 수 있는 `<model-viewer>` 라이브러리 |
| `현2222.glb` | 뷰어에서 불러오는 3D 모델 파일 |

## 3. 화면 구성

- 왼쪽: 회전·확대·축소·시점 이동이 가능한 3D 모델 뷰어
- 오른쪽: 모델 설명 패널
  - 모델 설명
  - 특징
  - 조작 방법
- 화면이 좁아지면 설명 패널은 모델 아래로 이동

설명 패널에는 현재 임시 문구가 들어 있습니다. 정확한 설명을 전달받으면 해당 내용을 교체하면 됩니다.

## 4. 모델 파일 처리

사용자가 제공한 파일은 다음 경로입니다.

`C:\Users\user\Downloads\현2222.glb`

프로젝트에도 같은 파일을 `현2222.glb` 이름으로 복사했으며, `index.html`의 `<model-viewer>`가 이 파일을 직접 참조하도록 설정했습니다.

```html
<model-viewer src="현2222.glb" ...>
```

파일 크기는 약 12.7MB이며, 로컬 서버에서 정상 응답되는 것을 확인했습니다.

## 5. Python 관련 확인 사항

처음 확인했을 때 일반 Python 실행 파일은 설치되어 있지 않았고, `py` 명령은 런처만 존재하는 상태였습니다.

대신 PC에 설치된 Blender의 내장 Python을 발견해 서버 실행에 사용했습니다.

```text
C:\Program Files\Blender Foundation\Blender 5.2\5.2\python\bin\python.exe
```

이 Python으로 `server.py`를 실행해 아래 주소가 정상 응답하는 것을 확인했습니다.

- `http://localhost:8000/`
- `http://localhost:8000/현2222.glb`

일반 Python을 설치한 경우에는 아래 명령으로도 실행할 수 있습니다.

```powershell
cd "C:\Users\user\Documents\미래일경험2기 전현탁"
py server.py
```

## 6. 모델이 보이지 않았던 원인 및 처리

초기에는 `<model-viewer>` 라이브러리를 외부 CDN에서 불러왔습니다. 인터넷 연결이나 브라우저 환경에 따라 이 라이브러리가 로드되지 않으면 모델이 표시되지 않을 수 있습니다.

이를 해결하기 위해 라이브러리 파일을 프로젝트 안에 직접 포함했습니다.

```html
<script type="module" src="model-viewer.min.js"></script>
```

따라서 현재는 3D 뷰어 라이브러리와 GLB 모델 모두 로컬 서버에서 제공됩니다.

## 7. 실행 방법

가장 쉬운 방법은 프로젝트 폴더의 `뷰어_실행.bat` 파일을 더블클릭하는 것입니다.

1. `뷰어_실행.bat` 실행
2. 자동으로 열리는 브라우저에서 `http://localhost:8000/` 확인
3. 사용 중에는 검은 실행 창을 닫지 않음
4. 종료할 때 검은 실행 창을 닫음

페이지가 이전 상태로 보이면 브라우저에서 `Ctrl + F5`를 눌러 강력 새로고침합니다.

## 8. 현재 상태

- 로컬 서버 응답 확인 완료
- `model-viewer.min.js` 로컬 제공 확인 완료
- `현2222.glb` 로컬 제공 확인 완료
- 3D 모델 렌더링 및 오른쪽 설명 패널 표시 확인 완료
